# Continue Here

- **Created**: 2026-02-26
- **Task**: Setting up Google Workspace MCP server for email/calendar in Claude Code
- **Branch**: main (up to date with origin, TODO.md modified)

## What's Done

- Researched SOTA for AI email/calendar automation (OpenClaw, MCP servers, agent frameworks, commercial tools)
- Decided on MCP + Claude Code approach (ruled out OpenClaw due to security concerns)
- Identified `taylorwilsdon/google_workspace_mcp` as the best single-server option (Gmail + Calendar in one, Python/uvx, `--tools gmail calendar` flag)
- Confirmed `uvx` is already installed (v0.9.22)
- Reviewed existing MCP config — brave-search, puppeteer, wharton-os, competitor-intel already configured
- Clarified difference between claude.ai's built-in Gmail/Calendar integrations vs. Claude Code's local MCP config
- Added setup steps to TODO.md
- Drafted and reviewed reply to JC's intro email to Tami (suggest Tuesday 10 ET intro call)

## What's Remaining

- User needs to create Google Cloud OAuth credentials before we can configure
- Once credentials are ready, run: `claude mcp add --transport stdio google-workspace --env GOOGLE_OAUTH_CLIENT_ID="<id>" --env GOOGLE_OAUTH_CLIENT_SECRET="<secret>" --env OAUTHLIB_INSECURE_TRANSPORT="1" -- uvx workspace-mcp --tools gmail calendar`
- Authenticate via browser OAuth flow
- Test: read inbox, check calendar, draft a reply

## Decisions Made This Session

- **OpenClaw rejected** — Cisco found third-party skills can exfiltrate data, skill repository lacks vetting. Security risk too high for something touching inbox.
- **Single server over two** — `google_workspace_mcp` covers both Gmail and Calendar vs. separate servers. One OAuth credential pair, one process, `--tools` flag keeps it focused.
- **Anthropic's official Gmail MCP** at gmail.mcp.claude.com does NOT exist for Claude Code — the research agent hallucinated it. The built-in integrations on claude.ai are web-only.

## Next Action

- Get Google Cloud OAuth credentials from user (Client ID + Client Secret)
- Run `claude mcp add` command above to add google-workspace to `~/.claude.json`
- Run `uvx workspace-mcp` auth flow to complete OAuth
- Smoke test: "What emails need my attention?" + "What's on my calendar today?"
